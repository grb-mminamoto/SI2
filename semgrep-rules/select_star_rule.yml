rules:
  - id: sql-select-star
    patterns:
      - pattern: |
          SELECT * FROM $TABLE
      - pattern: |
          SELECT * WHERE $CONDITION
    message: "Uso de SELECT * detectado. Evite usar SELECT * em consultas SQL."
    languages: [generic]
    severity: WARNING
