rules:
  - id: sql-select-star
    pattern: |
      SELECT $FIELD* FROM $TABLE
    message: "Uso de SELECT * detectado. Evite usar SELECT * em consultas SQL."
    languages: [generic]
    severity: WARNING
